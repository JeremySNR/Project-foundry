# AGENTS.md — fast orientation for AI agents working on this repo

> **Maintenance rule (non-negotiable):** any PR that adds, removes, or changes a
> feature MUST update this file in the same PR (and the README where user-facing).
> This document is the fast path for the next agent; if it drifts from the code,
> it is worse than useless. Treat it like a test: stale doc = failing build.

## What this project is

Foundry is a **governance control plane for AI coding agents**: a ticket goes in
(Linear / GitHub Issues / Jira), Foundry analyzes readiness, routes it to a repo,
classifies risk, produces a plan, gets a **human approval**, dispatches a coding
agent (Cursor, Claude Code, signed webhook, manual), then watches the PR/MR
(GitHub / GitLab) and re-dispatches the agent on CI failures or review requests —
policy-gated, capped, budgeted, and fully audited. It deliberately **stops at a
reviewed PR**: no auto-merge, no deploys, no migrations. The safety gates are the
product, not overhead. Canonical product statement: `VISION.md`.

**Looking for work, or about to start some?** The backlog lives in
[GitHub Issues](https://github.com/JeremySNR/Project-foundry/issues): bugs and
hardening are labelled `bug`, the strategic roadmap items `enhancement`. Assign
yourself (or comment) before starting, work one issue — or one clearly-scoped
slice of one — per PR, and split slices into sub-issues rather than new
top-level items. New top-level roadmap items need a human decision, not an
agent's.

## The run lifecycle (what the orchestrator does)

```
webhook intake → analyze (ready? AC present?) → enrich (which repo?) →
classify risk → plan → POLICY GATE → human approval → dispatch agent →
PR opens → diff-aware risk re-check on every push → CI fail / changes
requested → policy-gated retry (capped, budgeted) → merged/closed →
outcome recorded into delivery memory
```

Terminal states: merged, blocked (never retried), rejected, failed,
needs-clarification (re-triggerable — one *active* run per issue, not one ever).

## Honest map of the intelligence layers

Know what is smart and what is deliberately dumb before you "improve" anything:

| Layer | Mechanism | Where |
| --- | --- | --- |
| Ticket readiness / AC extraction | Regex + structure heuristics (default) **or** OpenAI structured output (`analyzer.provider: openai`). LLM failures degrade to the heuristic analyzer, recorded in the analysis `assumptions` — an OpenAI outage never fails intake. | `engines/analyzer.py`, `engines/openai_analyzer.py` |
| Work-type classification | Keyword-hit counting | `engines/analyzer.py` |
| Risk classification | Built-in keyword lists on ticket text + `fnmatch` globs on PR diff paths (default) **or** an LLM pass with cited evidence (`risk.provider: llm`). Both halves are operator-tunable without forking: the diff-stage path globs via `policy.sensitive_path_globs`, and now the ticket-text keywords via `risk.extra_sensitive_keywords` (issue #31, area name → extra keywords merged **on top of** the built-in floor via `merge_sensitive_keywords`; area names validated against `SENSITIVE_AREA_KEYS` at load). Strictly additive — extras can only flag *more* areas, never fewer, so they only escalate risk (invariant #1); no `SensitiveAreas` schema / `ApprovalRole` / Rego change (the keyword→area→level/roles mapping stays Python-side). The heuristics are a hard floor: the LLM may only escalate (add areas, raise the level), never downgrade — enforced in the classifier before the policy gate, so the policy engine/Rego are untouched. Evidence lands in the `RiskAssessment` artifact and `risk.escalated` event metadata. LLM failures degrade to the floor, recorded in the artifact. | `engines/risk.py`, `engines/llm_risk.py` |
| Repo routing | Tiered: explicit ticket association (conf 90) > delivery-memory priors (capped 89) > catalog IDF-token / keyword scoring (lexical-capped 89, stale-capped 65) > legacy YAML keywords. Lexical, not semantic. With `context.provider: code`, the synced file tree becomes a scored field and the bundle carries `RepoCodeFacts` (test layout, CODEOWNERS, manifests) + candidate files + inferred test commands; reasons cite concrete paths. Same confidence tiers/caps — code evidence never adds a tier. | `engines/enrichment.py`, `engines/code_context.py`, `catalog/`, `memory/priors.py` |
| Planning | Template rendering — steps are "Satisfy acceptance criterion: X" (default) **or** an LLM pass (`planner.provider: llm`) that turns the code-aware context (candidate files, `RepoCodeFacts`) into file-level steps, test locations, verify commands and `expected_files_or_areas`. The LLM is only consulted for runs the template planner would dispatch (ready + confident repo); identity/scope stay deterministic and the `agent_instructions` guardrail block is rendered by Foundry, never the model. LLM failures degrade to the template plan, recorded in the plan's `open_questions`. | `engines/planner.py`, `engines/llm_planner.py` |
| Delivery memory | Outcomes derived purely from the audit trail; Beta-smoothed routing priors (min 3 samples, >50% success); per-provider agent scorecards (which agent ships, by work type/repo); a scorecard-backed `recommend_provider` that turns the cards into one explainable provider pick for a work-type/repo (same priors guard rails: min-sample floor, >50%-merged gate, candidate allow-list); per-provider scorecard *trends* (`agent_scorecard_trends`) bucketing the same merge rate/retries/cost by day/week on one shared, zero-filled time axis so a card's direction of travel is visible; **learned dispatch has landed** (`agent.provider: auto`, issue #33): the orchestrator now *acts* on `recommend_provider` at first dispatch — see the `agents/` and `orchestrator.py` rows — so the scorecards are no longer reporting-only; ROI metrics API | `memory/` |

The deterministic heuristics are the no-key/offline reference implementations and
the test baseline; they are intentionally conservative, not unfinished LLM calls.

## Module map (`src/foundry/`)

| Path | What it is |
| --- | --- |
| `schemas/` | Pydantic contracts for every artifact a run produces (`extra="forbid"` — changes here are API changes; update consumers in the same PR) |
| `engines/` | analyzer / enrichment / risk / planner (see table above) + `llm.py` structured-LLM seam + `llm_risk.py` escalate-only LLM risk classifiers + `llm_planner.py` file-level LLM planner (degrades to the template planner) + `decomposition.py` — the deterministic epic **producer** (`decompose_epic`, #35): splits an epic ticket into one per-repo child ticket via an explicit *Repositories* section (`repo: scope` bullets, checkbox-tolerant) or, failing that, ≥2 `known_repositories`; carries the epic's acceptance criteria into each child and scopes each to one repo. <2 distinct repos ⇒ not an epic. The orchestrator depends on the `EpicDecomposer` protocol (`HeuristicDecomposer` wraps `decompose_epic`, the default) + `llm_decomposition.py` — the LLM-assisted decomposer (`decomposition.provider: llm`) that recovers epics described only in *prose* (neither heuristic shape present): it keeps the deterministic decomposer as a **non-overridable floor** (consulted only when the floor declines, so it can only *add* a split the floor missed, never remove/re-scope one), **grounds** every model-proposed repo against the ticket text/`known_repositories` (no invented repos), needs ≥2 grounded repos or it degrades to the floor, and degrades on any LLM failure — each child still independently gated, so no gate weakened |
| `policy/engine.py` | Default-deny policy gate, ~10 hard rules (readiness, repo confidence, sensitive areas, retry caps, budget, role-checked approvals; **every autonomous action also requires ≥1 recorded human approval** via the role-agnostic `approval_present` input — the human-in-the-loop promise as a gate rule, not just an orchestration check, issue #18; `auto_merge`/`production_deploy` denied unconditionally). Required approval roles are the union of the **risk-derived** roles (auth/infrastructure → engineering, customer_data/pii/payments → security) and any **per-repo** roles an operator scoped to the run's routed repo (`policy.repo_required_roles`, issue #31): the orchestrator resolves them and stamps `PolicyInput.repo.required_roles`, so both backends read the same field (no matrix logic duplicated in Rego, same pattern as the injected `repo_confidence_threshold`). Strictly additive — per-repo roles can only *add* a required approval, never drop a risk-derived one (invariant #1) — and enforced in `approve()` (refused before recording) as well as at the gate. The budget cap binds on **every** spending action (first `start_agent` + each `retry_agent`), comparing *projected* spend — recorded cost plus the next dispatch's `estimated_cost_per_dispatch` proxy for providers that don't report `cost_usd`. **Forbidden-path blocking is *not* here** — it lives in `orchestrator._forbidden_violations()` (diff-aware, sticky `BLOCKED`) and has no Rego mirror, so invariant #2 doesn't apply to it. Forbidden globs are global (`policy.forbidden_globs`) **plus** optional per-repo extras (`policy.repo_forbidden_globs`, keyed on the run's routed repo, issue #35) merged by `_forbidden_globs_for(repo)` — strictly additive, so per-repo scoping only ever makes the block stricter, never weakens the global floor (invariant #1). The same merged list seeds the agent's `do_not_modify` constraints at dispatch. **Per-*path* approval roles** are the other orchestrator-only path-scoped rule (`policy.path_required_roles`, issue #31/#35): a path glob → required approval roles, evaluated diff-aware in the same PR re-check (`_unapproved_path_roles`) — a diff touching a configured subtree whose role is not already covered by the run's approvers escalates the run to `REVIEW_REQUIRED` (audited `risk.escalated`, category `path_required_roles`). Like the forbidden-path block it has no Rego mirror and is strictly additive (it can only ever *escalate* to human review, never release a run), so invariant #2 doesn't apply; it complements `repo_required_roles` (resolved at intake from the routed repo) with a path axis the diff makes available. **Change-freeze / maintenance windows** are the *time* axis of the same orchestrator-only family (`policy.change_freeze_windows`, issue #31, the "time windows" dimension): the pure `policy/freeze.py` (`ChangeFreezeWindow` + `active_freeze(windows, now)`, recurring-weekly or absolute ranges, IANA tz, offline — the orchestrator injects a `clock`) is consulted in `_attempt_remediation` *before* an autonomous re-dispatch; an active window parks the run at `REVIEW_REQUIRED` (audited `risk.escalated`, category `change_freeze`) instead of firing the agent again. Strictly additive (a freeze can only ever hold an autonomous action for a human, never release one — invariant #1) and orchestrator-only with no Rego mirror (invariant #2 doesn't apply); the initial human-*approved* dispatch is deliberately not gated. Default empty = unchanged |
| `policy/foundry.rego` | OPA backend, selectable via `policy.provider: opa` (`OpaPolicyEngine`); the Python `LocalPolicyEngine` is the default. **Must change in lock-step** (machine-verified over shared vectors — see invariant #2). The confidence threshold is read from input, not hardcoded, so both backends honour the configured value |
| `policy/library/` | Starter policy library (issue #31): committed, tested preset configs (`baseline`, `soc2`, `change-management`, `pci-dss`) a buyer can copy into `foundry.yaml` instead of starting from a blank file. Built **only** from existing, already-gated knobs (`repo_confidence_threshold`, `forbidden_globs`, `repo_required_roles`, the N-of-M `min_approvals`/`repo_min_approvals`, `path_required_roles`, retry/budget caps) — no new policy mechanism, no `engine.py`/`foundry.rego` change, no audit write, so no lock-step concern. The `pci-dss` preset exercises the full modern surface (the two-person rule, per-path roles, protected key material). **Copy-to-adopt, never auto-applied** (loading the library can't change a running deployment's gate, so it can't weaken one — invariant #1); each preset is itself strict-or-stricter than the built-in defaults (a test asserts none drops a default forbidden glob). `library/__init__.py` is the pure, offline loader (`list_presets` / `load_preset_yaml` / `load_preset_settings` via `importlib.resources` + the real `Settings` validator) plus `effective_policy_summary` — the read-only `explain` data, which surfaces the **whole** gate a config resolves to (threshold, protected paths, per-repo roles, the N-of-M approval count + per-repo overrides, per-path roles, retry/budget caps) for **any** `Settings`, preset or live deployment config alike — and `compare_policy_strictness(subject, baseline) -> PolicyComparison` (+ `resolve_settings`, accepting a preset name *or* a config path), the **verification** counterpart to `explain`: a pure per-knob strictness diff (higher-is-stricter for `repo_confidence_threshold`/`min_approvals`; lower-is-stricter for `max_files_changed`/`max_agent_retries`/`max_cost_per_run` — `None` = no cap = weakest; superset-is-stricter for `forbidden_globs` and the per-repo/per-path role/glob maps **and the `change_freeze_windows`** (compared by a canonical, label-agnostic window key), the per-repo forbidden check merging the global set the orchestrator applies, the N-of-M count compared on the `max(global, per-repo)` *effective* value) that answers "does *my* config meet this baseline's floor?" — read-only, so no gate/`engine.py`/`foundry.rego` change — plus `comparison_to_dict`, the shared JSON serialiser for a `PolicyComparison` (one definition behind both the `foundry-policy check --format json` CLI output and the in-app `GET /metrics/policy/check`, so the two verdicts can't drift). `policy/cli.py` is the `foundry-policy` console entry (`presets` / `show <name>` copy-to-adopt / `explain <preset-name | --config PATH | path>` effective-knobs — now introspects **either** a reference preset (loaded pure) **or** the operator's *own* deployment config (a path argument, `--config PATH`, or `$FOUNDRY_CONFIG`, loaded with the process env so it resolves as the deployment does), answering "what does *my* gate resolve to?", not just a preset's, with `--format text` (default) or `--format json` (`{source, kind, policy}` on stdout) / `check --config PATH --against <preset-or-path>` — exits non-zero when the config is weaker than the baseline, a compliance-CI gate, with `--format text` (default) or `--format json` (machine-readable per-control verdicts + a `weaknesses` list on stdout; same exit code) — all decision-support only, never mutating a deployment's policy) |
| `orchestrator.py` | The state machine; writes every decision/artifact/approval as content-hashed audit rows. `approve()` pre-validates the approver's roles against the run's required approvals (derived from risk) and refuses *before* recording, so a void approval can't land on the trail and be blocked only at dispatch. **N-of-M approval matrix (issue #31):** `approve()` accumulates *distinct* approvals and only flips the run to `APPROVED` once `policy.min_approvals` (raised by any per-repo `policy.repo_min_approvals` via `max`) distinct humans have signed off — until then the run stays `WAITING_APPROVAL` with a `current_step` of `awaiting_approval (n/m)`; a second sign-off from the same person is refused, and dispatch reads the *union* of granted roles across all approvers. Lifecycle-only enforcement (like the forbidden-path block) — **no `PolicyInput`/`foundry.rego`/gate change**, so invariant #2 doesn't apply; default 1 = the historical single-approval path. **Learned dispatch (issue #33):** under `agent.provider: auto` the *first* dispatch picks the provider via `recommend_provider` over the configured `auto_candidates` (same min-sample floor + majority-merged gate the recommendation enforces), falling back to the configured default when no agent has earned a recommendation yet (kill switch = don't set `auto`); the pick is recorded as an explainable `selection` block on the `AGENT_STARTED` event. **Remediation reuses the original job's provider** — a retry never re-routes mid-run. The policy gate is provider-agnostic (no `PolicyInput`/`foundry.rego` field reads the provider), so this needed **no gate / Rego / `policy_vectors` / migration change** — only the multi-provider plumbing |
| `drivers.py` | RunDriver seam: inline in-process (the real one) vs Temporal. The `InlineDriver` owns the optional epic auto-decomposition (`auto_decompose_epics`, config `epics.auto_decompose`, **default off**, issue #35): when on, `start()` routes intake through `orchestrator.intake_epic` instead of `intake_and_plan`, so a multi-repo ticket arriving on the webhook/trigger path fans out into independently-gated child runs; it returns the parent (epic-root) run id, so the webhook callers' one-active-run-per-issue bookkeeping is unchanged. A ticket that doesn't decompose degrades to a single ordinary run, so the path is always safe to take |
| `workflows/` | Temporal version (durable waits for approval/PR). Signal-driven; the sequencing decisions are pure functions in `decisions.py` (offline-tested) and the workflow is a thin shell. Wait timeouts terminate cleanly (approval window → `BLOCKED`, PR window → `EXECUTION_FAILED`, both audited via `expire_pending_approval` / `expire_pending_pr`); unknown decision verbs are dropped in the signal handler (never a silent stop); `intake_and_plan` is idempotent under activity retries; PR observation loops on every push until the run leaves a PR-observable state. Each activity carries an **explicit per-activity retry/timeout policy** (`activity_options.py`, pure stdlib so it unit-tests offline): deterministic failures (`ValueError`/`ValidationError`/`OrchestratorError`) are classified non-retryable (fail fast), idempotent steps (intake, `record_pr`) retry patiently, the heaviest step (intake) gets the longest budget — `workflow.py` turns each spec into the Temporal `RetryPolicy`/timeout. An activity that **exhausts its retry budget** (raising `ActivityError`) is caught by the workflow, which runs the `fail_run` **compensation activity** (`orchestrator.fail_run` → any still-active run to `EXECUTION_FAILED`, audited `agent.failed`/`workflow_irrecoverable_error`, in-flight job cancelled, idempotent and terminal-safe per invariant #7) and then re-raises so the workflow still surfaces as Failed — the run never strands active forever. **Now proven against a real server** (issue #37): the E2E `test_temporal_workflow.py` runs in two modes — the in-memory time-skipping harness (default; covers the multi-day approval/PR-window *timeout* paths) **and** a real `temporalio/auto-setup` server when `FOUNDRY_TEMPORAL_TEST_ADDRESS` is set (`WorkflowEnvironment.from_client`; the cases that only settle via a multi-day durable-wait fast-forward — the approval/PR-window timeouts and the post-PR-open quiet window — self-skip since a real server doesn't skip time, leaving the prompt signal-driven paths incl. approve→dispatch→PR→merge→complete). The `temporal` CI job boots the docker-compose `temporal` profile (Postgres-backed auto-setup), waits via `scripts/wait_for_temporal.py` (blocks until the `default` namespace serves), and runs the signal-driven E2E against `localhost:7233`. The offline `test` job still uses the time-skipping path (skips where the test-server binary isn't fetchable) |
| `agents/` | Provider abstraction: `manual`, fake, `cursor_cloud`, `cursor_via_linear`, `claude_code` (GitHub Actions `workflow_dispatch`), `webhook` (HMAC-signed). All go through one `create_job` path with a secret-leak scan, and expose `cancel_job` — a human `stop`/`reject` best-effort cancels the in-flight job (Cursor cancel API; no-op for `manual`/`webhook`/`claude_code`) so a stopped run stops spending, recorded as an `AGENT_CANCELLED` audit event. **Learned dispatch (`agent.provider: auto`, issue #33):** `build_provider_registry` (in `api/app.py`) builds *every* `agent.auto_candidates` provider plus the `agent.auto_fallback` (fail-closed on missing credentials, like any provider) into a name→provider registry handed to the orchestrator; the orchestrator is now provider-*registry* aware (cost-refresh, cancel and superseded-cancel resolve the provider by the recorded `job.provider`, not a single singleton — so a run is always reconciled against the agent that actually ran it). A single configured agent is byte-for-byte the old path (one-entry registry, no scorecard query) |
| `connectors/` | Trackers: Linear, GitHub Issues, Jira. SCMs: GitHub, GitLab. Both fetch the changed-file list via an injected transport (`github_transport` / `gitlab_transport`, GitLab paging `/diffs`) so file-based gates see the full diff; **no token ⇒ diff-blind, gates skipped**. Chat notifications: `RunNotifier` seam (`connectors/notify.py`) with a `SlackNotifier` (`connectors/slack.py`, `slack_transport` → `chat.postMessage`) that posts the interactive approval message (buttons whose `action_id`/`value` the inbound `api/slack.py` parser consumes — the wire contract `SLACK_ACTION_PREFIX`/`SLACK_DECISIONS` lives in `connectors/slack.py`, re-exported by `api/slack.py`), an **N-of-M progress nudge** (`RunNotifier.approval_progress` / `ApprovalProgress` + `format_approval_progress_comment`, issue #31) re-pinging the next approver on the tracker + chat after a *partial* sign-off so a two-person-rule run doesn't go silent at `awaiting_approval (1/2)` (fired from `orchestrator.approve()` only when `approver_count < min_required`; presentation-only, default `min_approvals=1` never reaches it), and status updates (parked/blocked/PR open/merged). Best-effort like the tracker write-back; **fail-closed: wired only when both `FOUNDRY_SLACK_BOT_TOKEN` and a channel are set**. `transport.py` is the HTTP seam (fakes in tests). Jira webhooks have no body signature, so the shared `FOUNDRY_JIRA_WEBHOOK_SECRET` is an approver-level credential (the actor identity comes from the payload): it is read from the `X-Foundry-Webhook-Token` header only, with `?token=` query delivery off unless `tracker.jira_allow_query_token: true` (see SECURITY.md) |
| `catalog/` | `foundry-catalog sync` — GitHub org metadata sweep feeding the catalog enricher (stateful, budget-aware, resumable). `--code-facts` (implied by `context.provider: code`) adds per-repo code facts: file tree via the Git Trees API, CODEOWNERS, root manifests; derivation logic is pure functions in `catalog/code_facts.py` |
| `memory/` | Run outcomes, routing priors, agent scorecards, per-provider scorecard *trends* over time (`scorecards.agent_scorecard_trends` — the same merge rate/retries/cost bucketed by day/week per provider, so "is this agent improving or sliding?" is answerable, not just a flat all-time average), scorecard-backed provider recommendation (`scorecards.recommend_provider`), delivery metrics (`delivery_metrics` org-wide, `delivery_trends` bucketed over time, `delivery_by_repo` grouped by routed repo — the "where work ships, stalls and spends" cut, NULL-repo runs under an `(unrouted)` sentinel — and `delivery_by_repo_trends`, the per-repo cut of `delivery_trends`: each repo's zero-filled series on one shared time axis + its window totals, so "is *this repo* speeding up or stalling?" is answerable, not just the org-wide trend), the live `fleet_status` snapshot + its three in-flight queue drill-downs — `approval_queue` (human-parked runs, wait age + approval SLA), `execution_queue` (in-flight `AGENT_RUNNING` runs, run-time age dated from the latest `AGENT_STARTED` + execution SLA, the hung/runaway-agent signal), and `review_queue` (open `PR_OPEN` runs with a review-latency age from `PR_OPENED` + review SLA **and** an `inactive_seconds` "stale since last push" age from the latest `PR_OPENED`/`PR_UPDATED` + a separate staleness SLA, the abandoned-vs-active-PR signal) — plus the **failure-side** complement `failure_queue` (the triage feed behind the strip's blocked/failed counts: every run in a terminal-failure state — `BLOCKED` or `EXECUTION_FAILED` — that failed within a recent window, each with how long ago it failed (dated from the `RUN_BLOCKED`/`AGENT_FAILED` marker event, per `_FAILURE_EVENTS_BY_STATUS`) and *why* (the `category`/`reason` from that event's audit metadata), **newest first** — unlike the three waiting queues it is bounded to a window and recency-ordered, since a failed run is terminal and never drains; read-only, blocks nothing, a blocked run stays blocked per invariant #7); `foundry-memory backfill / show-priors / show-scorecards / show-scorecard-trends / recommend-agent / fleet / failures` — the last two (issue #37) are the **offline twins** of `GET /metrics/fleet` and `GET /metrics/failures` (read the DB directly + call the same `fleet_status` / `failure_queue` derivations the API serves, honouring the same `dashboard.*_sla_seconds` knobs, so the CLI and API verdicts can't drift; for an on-call/auditor with DB access but no running API or bearer token, mirroring how `foundry-evidence` is the offline twin of the evidence endpoints) |
| `api/` | FastAPI: signed webhooks (Linear/GitHub/Jira/GitLab), Slack interactivity approvals (`POST /webhooks/slack` — `api/slack.py`; Slack v0 request-signing + replay-age in `api/security.py`; approve/reject/stop buttons drive the same `_apply_decision` path; actor = Slack-signed `user.id`, approvers keyed by Slack user id; fail-closed on `FOUNDRY_SLACK_SIGNING_SECRET`), REST approvals, run timeline, `GET /runs/{id}/epic` (epic view for the parent/child run model #35 — resolves the epic root so it works on a child too, then returns the root run, its children, and the `compute_epic_rollup` summary; token-gated like the timeline), `GET /runs/{id}/epic/evidence` (the cross-run compliance export for that epic — the parent + every child as one evidence pack with the rollup and aggregate integrity/coverage; resolves the root like the epic view, JSON, `?format=html`, or `?format=pdf` (the optional `[pdf]` extra)), `GET /metrics/delivery`, `GET /metrics/delivery/trends` (the same aggregates bucketed by `day`/`week` over time), `GET /metrics/delivery/by-repo` (the same finished-run outcomes grouped by routed repo — PRs shipped/blocks/merge rate/spend/time-to-merge per repo, the "which repos ship, which stall, where the spend goes" cut; unrouted runs bucket under an explicit `(unrouted)` label; sorted most-shipping then most-active then name; read-only, issue #37), `GET /metrics/delivery/by-repo/trends` (the same per-repo outcomes bucketed by `day`/`week` over time — the repo dimension of `/metrics/delivery/trends`: each repo carries a zero-filled `series` on one shared time axis plus its window totals, so "is this repo shipping more or stalling?" is visible per repo, not just org-wide; same `(unrouted)` sentinel + sort order, read-only, issue #37), `GET /metrics/agents` (per-provider scorecards), `GET /metrics/agents/trends` (the same scorecards bucketed by `day`/`week` over time — is each agent improving or sliding, not just its flat all-time average; reporting only), `GET /metrics/agents/recommendation` (the scorecards turned into one explainable provider pick for a `work_type`/`repo` — decision-support only, nothing dispatches), `GET /metrics/fleet` (a live, no-window snapshot of every run's *current* state — runs in flight, approval-queue depth + **oldest wait age** + SLA-breach count, agents running + **oldest execution age** + execution-SLA-breach count, PRs open + **oldest review age** + review-SLA-breach count, spend committed by in-flight runs — read from `FoundryRun`, distinct from the finished-run-only delivery aggregates; in `memory/metrics.py`), `GET /metrics/approvals` (the approval-queue drill-down behind that count — every run parked on a human, oldest first, each with its `waiting_seconds` and `sla_breached` flag; `approval_queue` in `memory/metrics.py` dates each wait from the audit event marking the run's *last transition into its current parked status* — *not* `updated_at` (which carries `onupdate=utcnow`, so any later row touch, e.g. an N-of-M partial sign-off's `APPROVAL_GRANTED`, would reset the SLA clock). Markers are mapped **per status** (`_WAIT_START_EVENTS_BY_STATUS`): `WAITING_APPROVAL` ← `APPROVAL_REQUESTED`; `REVIEW_REQUIRED` ← the latest of `RISK_ESCALATED` (sensitive-area / path-role / **diff-too-large** escalation), `AGENT_FAILED` (failed re-dispatch), or `RUN_BLOCKED` (remediation denied); `NEEDS_CLARIFICATION` has no transition event and dates from the run's immutable `created_at` (exact — it is only ever parked at intake). The oversized-diff `REVIEW_REQUIRED` transition in `orchestrator._next_status_for_pr` now records a `RISK_ESCALATED` event (`category: diff_too_large`) — previously a silent status flip — closing both the wait-start marker gap and an audit-trail gap. The SLA threshold is the read-only `dashboard.approval_sla_seconds` config knob, default unset = no breach signal, blocks no run, issue #37), `GET /metrics/executions` (the machine-state complement to `/metrics/approvals` — the drill-down behind the fleet strip's `agents_running` count: every run currently in `AGENT_RUNNING` (agent dispatched, no PR opened yet), oldest first, each with its `running_seconds` and `sla_breached` flag; `execution_queue` in `memory/metrics.py` dates each run-time from the run's **latest `AGENT_STARTED` event** — the dispatch that put it in flight, so a remediation retry re-dispatch resets the clock to the current attempt — falling back to the immutable `created_at` only if no dispatch event exists. The SLA threshold is the separate read-only `dashboard.execution_sla_seconds` knob — the hung/runaway-agent signal — default unset = no breach signal, blocks no run; scoped to `AGENT_RUNNING` only, since `PR_OPEN`/`REVIEW_REQUIRED` are waiting on CI/reviewers, not the agent, issue #37), `GET /metrics/reviews` (the review-side complement to `/metrics/approvals` + `/metrics/executions` — the drill-down behind the fleet strip's `prs_open` count: every run currently in `PR_OPEN` (agent shipped a PR, now awaiting review/CI), oldest first, each with its `unreviewed_seconds` and `sla_breached` flag; `review_queue` in `memory/metrics.py` dates each PR's review age from the run's **`PR_OPENED` event** — when the PR first opened; later pushes emit `PR_UPDATED`, so a re-push does **not** reset the review clock — falling back to the immutable `created_at` only if no `PR_OPENED` event exists. The SLA threshold is the separate read-only `dashboard.review_sla_seconds` knob — the "PRs sitting unreviewed for N hours" signal, the one place delivery stalls after Foundry stops at a reviewed PR — default unset = no breach signal, blocks no run; scoped to `PR_OPEN` only, since `REVIEW_REQUIRED` is a human-decision park already aged by `/metrics/approvals`. Each entry **also** carries an `inactive_seconds` "stale since last push" age dated from the **latest of `PR_OPENED`/`PR_UPDATED`** (a fresh push resets it; by construction `inactive_seconds <= unreviewed_seconds`) with its own `stale_breached` flag/`stale_breaches` summary + `oldest_inactive_seconds`, gated by the **separate** read-only `dashboard.review_stale_sla_seconds` knob (`reviews_stale`/`oldest_inactive_seconds` also on `/metrics/fleet`) — the "which open PRs has nobody touched in N hours?" abandoned-vs-active signal, default unset = inert; issue #37), `GET /metrics/failures` (the **failure-side** complement to the three queue drill-downs — the incident/triage feed behind the strip's blocked/failed counts: every run in a terminal-failure state (`BLOCKED` or `EXECUTION_FAILED`) that failed in the last `days` (default 7), **newest first**, each with how long ago it failed (dated from the `RUN_BLOCKED`/`AGENT_FAILED` marker event) and *why* (the reason from that event's audit metadata); `failure_queue` in `memory/metrics.py`. Unlike the three waiting queues — which age runs still in flight and order oldest-first — a failed run is terminal and never leaves the state, so this is a recency-ordered feed bounded to a recent window, not an all-time list. Read-only, changes no gate: a blocked run stays blocked (invariant #7), re-triggering the ticket starts a fresh run; issue #37), `GET /metrics/policy` (the effective policy gate this deployment resolves to — the read-only, in-app twin of `foundry-policy explain --config`: the `effective_policy_summary` of the live `Settings` (confidence threshold, protected paths, per-repo/per-path required roles, the N-of-M approval counts, retry/budget caps) plus which backend (`local`/`opa`) enforces it, so an operator/auditor sees *what the gate is* on the dashboard without CLI access; `{configured, provider, policy, active_freeze}` — `configured:false`/`policy:null` when the app was built without a `Settings` (a bare `create_app`); the **one live, time-dependent field is `active_freeze`**, evaluated *at request time* against the app clock with the same `active_freeze()` the orchestrator uses before holding an autonomous re-dispatch — `null` when no change-freeze window is in effect, otherwise the active window's `{description, reason}`, answering the "*is a freeze active right now, and why*" question the static `policy.change_freeze_windows` list can't; read-only, changes no gate, issue #31), `GET /metrics/policy/check` (the **compliance verdict** of the live gate against the configured baseline — the always-on, in-app twin of `foundry-policy check --against`: when `dashboard.policy_baseline` is set (a preset name or config path) it returns the per-control `findings`, overall `ok`, and `weaknesses` list of whether the deployment's gate is at least as strict as that baseline, computed at build time from the **same** `compare_policy_strictness` the CLI `check` exits on (shared `comparison_to_dict` serialiser, so the dashboard verdict can't drift from the CI gate's); `{configured:false}` when no baseline is set; an unresolvable baseline fails loud at `app_from_settings` startup so a compliance check never silently "passes"; read-only, changes no gate, issue #31), `GET /epics` (every epic root — a run other runs point at via `parent_run_id` — with the `compute_epic_rollup` status and its child runs; the dashboard epic board reads it, ordinary single-repo runs are omitted; same `run`/`rollup`/`children` shape as `GET /runs/{id}/epic`), `GET /runs/{id}/evidence` (single-run compliance evidence pack, JSON, `?format=html`, or `?format=pdf` (the optional `[pdf]` extra)), `GET /evidence` (org-wide evidence archive over a date range — every run in the window as the same per-run pack, plus a rollup of aggregate integrity, status breakdown, and per-control coverage; bound with ISO `from`/`to` — `from` inclusive, `to` exclusive, a date-only `to` covers the whole day — or `days`, default last 90; JSON, `?format=html`, or `?format=pdf` (the optional `[pdf]` extra)), and a zero-build HTML dashboard at `/dashboard` (a live fleet strip — now carrying the oldest approval-wait age + SLA-breach count, the oldest execution age + execution-SLA-breach count, and the oldest review age + review-SLA-breach count, an **approval-queue panel** from `GET /metrics/approvals` (parked runs oldest-first with wait age, breaches highlighted, click-through to the timeline), an **execution-queue panel** from `GET /metrics/executions` (in-flight agent runs oldest-first with run-time age, breaches highlighted, click-through to the timeline), a **review-queue panel** from `GET /metrics/reviews` (open PRs oldest-first with review-latency age, breaches highlighted, click-through to the timeline), a **failure/triage panel** from `GET /metrics/failures` (recently blocked/execution-failed runs newest-first with how long ago they failed and why, click-through to the timeline), run board with an approval-queue filter, the metrics strip, the delivery-trend table, the agent scorecards, a per-agent merge-confidence trend sparkline strip from `GET /metrics/agents/trends`, a delivery-by-repo table from `GET /metrics/delivery/by-repo`, an epic board rolling up multi-repo runs from `GET /epics`, a **policy-gate panel** from `GET /metrics/policy` (the effective gate this deployment enforces — the in-app twin of `foundry-policy explain`; lists the configured change-freeze windows and shows a **"CHANGE FREEZE ACTIVE"** banner when `active_freeze` is set), a **compliance-check panel** from `GET /metrics/policy/check` (a control-by-control PASS/FAIL of the live gate against the configured `dashboard.policy_baseline`, the in-app twin of `foundry-policy check`; hidden when no baseline is set), and the per-run timeline). Everything token-gated, **fail-closed** (no credential configured = endpoints disabled). Auth accepts the static bearer `FOUNDRY_API_TOKEN` **and/or**, when configured, an **OIDC JWT** (`api/oidc.py`, issue #34): `_require_api_token` tries the constant-time static-token compare first, then verifies the bearer as an OIDC token against the configured IdP. OIDC is opt-in (`auth.oidc` — `issuer`/`audience`/`jwks_uri`, all three required together or `Settings` load fails), additive (the static token is unchanged), and hardened (JWKS fetched through an injected transport seam so it tests offline; RS256-only algorithm allow-list shuts off `alg:none`/HS-confusion; `iss`/`aud`/`exp` required, bounded clock-skew leeway; unknown/missing `kid` refused). `pyjwt[crypto]` is the lazily-imported optional `[oidc]` extra — the default no-OIDC path never imports it; a deployment that sets OIDC without the extra fails loud at startup. **OIDC approver binding + IdP-group → role mapping (#34):** on the REST `POST /runs/{id}/approval` path, when the request authenticated via OIDC the approver *identity* is the **verified** subject claim (`auth.oidc.subject_claim`, default `email`, fallback `sub`), **not** the request body — a body `user` disagreeing with the verified subject is refused (403) — and the approver's roles are the committed `approvers[identity]` grant **unioned** with the roles the verified group claim (`auth.oidc.group_claim`, default `groups`) maps to via the committed `auth.oidc.group_role_map`. Both role sources are committed config; only the signed identity/group *claims* come from the token, so invariant #5 holds and the gate is *strengthened* (a spoofable body actor is removed), never weakened — a group mapped to the wrong role still can't approve a run that requires another. The static-token path is byte-for-byte unchanged (actor = body `user`, roles from `approvers`, group map inert); `_require_api_token` returns the verified claims (or `None` for the static path) so the endpoint can do the binding. **Dashboard browser OIDC login / SSO (#34):** `api/oidc_login.py` adds the OAuth2 **authorization-code-with-PKCE** browser flow — `GET /dashboard/login` (302 to the IdP authorize URL with `state`/`nonce`/PKCE `S256`, stashed in a short-lived signed login-state cookie — no server-side store), `GET /dashboard/auth/callback` (verifies `state`, exchanges the `code` at the IdP token endpoint over an injected transport seam, verifies the id_token via the shared `OidcVerifier` with audience = client id, checks `nonce`, mints a signed **session cookie** carrying the verified subject), `GET /dashboard/logout` (clears the local session cookie and, when **RP-Initiated Logout** is configured — `auth.oidc.end_session_endpoint` (+ optional `post_logout_redirect_uri`), #34 — redirects on to the IdP's end-session endpoint so the SSO session is terminated too, not just the local cookie; the logout URL carries `client_id` in lieu of an `id_token_hint`, so the session cookie's contents are unchanged, and is built only from committed config (no open-redirect surface); unset ⇒ byte-for-byte the prior clear-cookie-and-return-to-`/dashboard` behaviour). The session cookie is signed by `api/sessions.py` (`SessionSigner`, HMAC-SHA256, stdlib only — integrity + expiry, *not* encryption) and authenticates the dashboard's **read** calls (`_require_api_token(..., allow_session=True)`); it is deliberately **rejected on the approval endpoint** (`allow_session=False`) so an auto-sent cookie can't be tricked (CSRF) into driving an approval — approvals still need a bearer/webhook. Login is additive, opt-in, fail-closed: wired only when the non-secret parts (`auth.oidc` `client_id`/`authorization_endpoint`/`token_endpoint`/`redirect_uri`, all-or-nothing at load) **and** the env-only secrets (`FOUNDRY_OIDC_CLIENT_SECRET`, `FOUNDRY_SESSION_SECRET`) are set — otherwise the routes 403 and the dashboard falls back to the pasted-token UX (`render_dashboard` injects an `oidc_login`/`session` flag pair so the page shows "Sign in with SSO" / skips the token box when already signed in). Token exchange uses httpx (`[http]`); tests inject the exchanger + JWKS fetch so the whole flow runs offline. Remaining #34 follow-ups: binding the *actor* on the signed-webhook paths is moot (already from the signed payload); `org_id`/row-level isolation and SCIM are open. Replayed/redelivered webhooks are dropped by a **durable, bounded dedup** table (`api/dedup.py`, keyed on `(provider, delivery_id)`, TTL-pruned); an optional fail-closed replay-age check rejects deliveries older than `webhook.replay_max_age_seconds` for providers that carry a timestamp (Linear's `webhookTimestamp`). Coarse per-client rate limiting (`api/ratelimit.py`) fronts the webhook and API surfaces via an ASGI middleware — fixed-window, two independent buckets (`webhook`/`api`), on by default, configurable under `rate_limit:`; **per-process** (in-memory), keyed by the direct peer address |
| `compliance/` | Compliance evidence packs: read-only packaging of a run's chain. `evidence.py` `build_evidence_pack` assembles one run's pack + `verify_integrity` (recomputed artifact content-hashes + audit-sequence continuity + a **cross-row linked hash chain** over the audit events — `_verify_audit_chain` recomputes the chain where each event's `content_hash` commits to the previous event's, so a dropped/reordered/edited/inserted row is detectable; events written before the chain column existed read as `present:false` and don't fail verification. The link fn is `audit.events.audit_event_chain_hash`, shared with the write path so they can't drift; tamper-*evidence*, not tamper-proof — no external anchor, #36/#13/#10); `build_evidence_archive` rolls every run in a `created_at` date range (`since` inclusive, `until` exclusive, either bound optional) into one org-wide export with aggregate integrity / status / per-control coverage; `build_epic_evidence_pack` is the cross-run cut of the same rollup (#35) — an epic's full chain as one export: the parent run plus every child pack, the `compute_epic_rollup` status, explicit root/child linkage, and the same aggregate summary (shared `_summarise_packs` helper); `render_evidence_html` / `render_archive_html` / `render_epic_evidence_html` are the zero-build HTML renderers. `pdf.py` (#36) holds the matching `render_evidence_pdf` / `render_archive_pdf` / `render_epic_evidence_pdf` renderers — built from the **same** pack dicts as the HTML renderers (no second data path), using `fpdf2` (the optional, lazily-imported `[pdf]` extra, like `[oidc]`/`[crypto]`: the JSON/HTML exports and the offline core never import it; a PDF request without it raises `PdfRenderingUnavailable`). Core-font (Helvetica) only and text sanitised to latin-1, so the path is offline and can't crash on arbitrary ticket text. `controls.py` holds the control→evidence-section mappings (config, overridable via `compliance.control_mappings`; section names validated at load). `cli.py` is the `foundry-evidence` console entry point — the **offline twin** of the three evidence endpoints (`foundry-evidence run/epic/archive`, `--format json|html|pdf`, `--output PATH`): it reads the DB directly and calls the same builders/renderers, so an auditor gets a pack without the API or a bearer token; control mappings come from committed config like the API, the `epic` subcommand resolves the epic root first (works on a child id), and `archive` reuses the API's `from`-inclusive/`to`-exclusive/date-only-`to`-covers-the-day bounds |
| `db/models.py` | SQLAlchemy: runs, artifacts, audit events, policy decisions, agent jobs, repo catalog, run outcomes, webhook deliveries (dedup). **Single-tenant** — no org/tenant columns. `foundry_runs.parent_run_id` is a nullable self-FK for epic decomposition (#35): a child run points at its parent epic, single-level (the orchestrator refuses to nest). `foundry_audit_events.content_hash` is a nullable cross-row chain hash (#36): each event commits to the previous event's hash for its run (assigned by the `db/base.py` flush hook alongside `sequence`); nullable so pre-chain rows read back cleanly |
| `epics.py` | Read side of the parent/child run model (#35): `compute_epic_rollup` — a pure function deriving an epic's status (`empty` / `in_progress` / `complete` / `partial` / `failed`) and per-bucket counts from its children's `RunStatus` values, mirroring the `ACTIVE_RUN_STATUSES` / terminal sets so it can't drift from the lifecycle. `FoundryOrchestrator.list_epics()` is the matching query side — the epic roots (runs with ≥1 child) that feed the `GET /epics` dashboard board. `EpicIntakeResult` is the producer's return shape. The producer itself is `orchestrator.intake_epic(ticket, ...)` (#35): it `decompose_epic`s the ticket, opens the parent (epic root) run for the epic ticket, then one **independently-gated** child run per repo via `intake_and_plan(..., parent_run_id=...)` — each child analysed/risk-classified/planned/gated and parked for its own approval, no gate weakened. A non-epic ticket degrades to a single ordinary run with no children. The producer is now also reachable from the webhook/trigger auto-path behind a default-off flag (`epics.auto_decompose`, wired through `InlineDriver.auto_decompose_epics`); LLM-assisted decomposition is the remaining future slice |
| `audit/` | SHA-256 content hashing for artifacts and events, plus `audit_event_chain_hash` — the linked-hash-chain step that ties each audit event to the previous one in its run's trail (assigned at flush time in `db/base.py`, recomputed for verification in `compliance/evidence.py` — one definition, two call sites, #36) |
| `config.py` | Layering: built-in defaults → YAML (`FOUNDRY_CONFIG`) → env vars. Behaviour in YAML (committed), secrets in env (never committed). Example: `foundry.example.yaml` |

Other locations: `tests/fixtures/` (webhook payloads — spec-derived from the
providers' webhook docs, replaceable by redacted live captures — pinning every
payload mapping), `migrations/` (Alembic — the **single** schema owner on Postgres, run by the
Docker entrypoint on startup; SQLite dev/test is bootstrapped in-process by
`db.base.init_schema`, which create_alls only on SQLite so it never strands a
later `alembic upgrade head`),
`examples/claude-code-runner.yml` (reference agent workflow), `scripts/demo.py`
(offline narrated end-to-end demo — the fastest way to see the whole loop).

## Hard invariants — do not route around these

1. **Never weaken a gate.** PRs that loosen a policy rule, approval requirement,
   or audit write don't merge. Change rules explicitly, with tests, or not at all.
2. **Python policy engine and `foundry.rego` change together**, with tests in
   `tests/test_policy_engine.py` and `foundry_test.rego`. Lock-step is
   machine-verified: shared vectors in `tests/data/policy_vectors.json` run
   through `LocalPolicyEngine` (`tests/test_policy_parity.py`) **and** through
   `opa eval` (`scripts/policy_parity.py`, in the OPA CI job) against the same
   expected decisions — add a vector when you add a rule, on both sides.
3. **No network in core tests.** `pytest` must pass offline with zero API keys.
   New external calls need a transport seam + a fake.
4. **The policy gate is default-deny**; an unrecognised action is refused.
5. **Approver roles live in committed YAML / config**, never in request payloads.
6. **Secrets never reach agent prompts** — job inputs pass the leak scan in
   `agents/provider.py`.
7. **Forbidden-path blocks are never retried.** Blocked stays blocked — and that
   must hold under concurrency too: every run state-transition reads the run row
   with `_require_run(..., lock=True)` (`SELECT … FOR UPDATE`, no-op on SQLite,
   real on Postgres) and re-verifies status under that lock before writing. A
   (re)dispatch that finds the run already terminal (e.g. a human `stop` won the
   race) records the launched job, cancels it, and never reverts the status. The
   `(run_id, sequence)` audit index is unique so a duplicate sequence fails loud.
8. New config goes in `foundry.example.yaml` with a comment; secrets are env vars.

## Dev commands

```bash
pip install -e ".[test]" && pytest          # full offline suite (~500 tests)
ruff check src tests                        # lint
opa test src/foundry/policy -v --ignore '*.yaml'  # Rego tests (needs opa CLI; ignore preset configs)
python scripts/demo.py                      # offline end-to-end demo
foundry-policy presets                      # browse the starter policy library
foundry-policy check --config foundry.yaml --against soc2  # verify your config meets a baseline (exits non-zero if weaker; add --format json for CI)
make dev                                    # uvicorn API on :8000, SQLite
docker compose up --build                   # API + Postgres (+ Temporal profile)
alembic upgrade head                        # migrations (Postgres)
```

Gated suites (skipped by default): Temporal (the time-skipping E2E needs the
fetchable test-server binary; the **real-server** E2E runs in the `temporal` CI
job — `docker compose --profile temporal up -d temporal`, then
`FOUNDRY_TEMPORAL_TEST_ADDRESS=localhost:7233 pytest tests/test_temporal_workflow.py`),
Postgres smoke, live E2E (`FOUNDRY_E2E=1` + real credentials — never in CI).

## Known limitations (so you don't rediscover them)

- Context/routing is lexical (token overlap), not semantic — no AST or embeddings.
  The `code` provider reads file trees/CODEOWNERS/manifests, but `static`/`catalog`
  still read no code, and code facts only exist after a `--code-facts` sync.
- Plans are templated by default; `planner.provider: llm` adds a file-level
  planner that consumes the code-aware context (candidate files, code facts).
  It populates `expected_files_or_areas`, but the plan-vs-diff gate that would
  consume it is not built yet.
- Risk-from-ticket is keyword matching by default (risk-from-diff globs are
  precise); `risk.provider: llm` adds judgment + cited evidence, escalate-only.
  Both heuristic halves are operator-extensible without forking — the diff globs
  via `policy.sensitive_path_globs`, the ticket-text keywords via
  `risk.extra_sensitive_keywords` (#31, merged onto the built-in floor, strictly
  additive). Genuinely new, dynamically-named risk *categories* (beyond the seven
  fixed `SensitiveAreas`) remain open on #31, alongside live user-loadable OPA
  bundles.
- Single-tenant DB (no `org_id` / row-level isolation yet). API auth accepts
  OIDC JWTs as well as the static bearer token (#34), and the OIDC REST-approval
  path now **binds the approver identity to the verified token** and **maps IdP
  groups to approver roles** via committed config (`auth.oidc.group_role_map`) —
  so SSO group membership can grant approval authority, the group→role map stays
  committed config (invariant #5), and the gate's role requirements are
  unchanged. The dashboard's **browser-side OIDC login (SSO)** has now landed
  (`api/oidc_login.py`: authorization-code + PKCE, signed session cookie via
  `api/sessions.py`) — the session cookie authenticates the dashboard's reads
  but is rejected on the approval endpoint (CSRF-safe), so it never widens the
  human gate. **RP-Initiated (federated) logout** has landed too
  (`auth.oidc.end_session_endpoint` (+ optional `post_logout_redirect_uri`),
  #34): `/dashboard/logout` ends the IdP SSO session, not just the local cookie,
  so a shared workstation can't silently re-authenticate — additive, opt-in,
  default-off. Still no SCIM, no OIDC token *refresh*, and no `org_id`/row-level
  isolation. Coarse per-client rate limiting fronts the webhook + API surfaces.
- Temporal driver now CI-proven against a **real** server (issue #37); the
  inline driver remains the production path. The known workflow holes
  (wait-timeout handling, decision-verb validation, intake idempotency,
  single-shot PR observation) are fixed and CI-tested under the time-skipping
  environment, activity retry/timeout policies are now explicit per-activity
  (`activity_options.py`), a compensation activity now auto-fails a run
  (`fail_run`) when an activity exhausts its retries so it can't strand active,
  and the workflow now also runs end-to-end against the real
  `temporalio/auto-setup` server in the `temporal` CI job (the docker-compose
  Temporal profile) — so durability is proven against the production server
  binary, not just the in-memory time-skipping harness. (Multi-day *wait*
  behaviour under wall-clock time stays covered by the time-skipping job, since
  a real server doesn't skip time.) The fleet
  dashboard now ages all three in-flight parked states — the approval queue
  (`/metrics/approvals`), the agent-execution queue (`/metrics/executions`), and
  the PR review queue (`/metrics/reviews`, "PRs sitting unreviewed for N hours",
  `dashboard.review_sla_seconds`) — each read-only with its own optional SLA. The
  review queue additionally carries a "stale since last push" age per PR
  (`inactive_seconds`, dated from the latest `PR_OPENED`/`PR_UPDATED`) with its own
  `dashboard.review_stale_sla_seconds` knob (`reviews_stale`/`oldest_inactive_seconds`
  on the strip) — distinguishing an actively-pushed PR from an abandoned one,
  distinct from the open-age review SLA. Alongside those three *in-flight* queues
  the dashboard also has a **failure-side** triage feed (`/metrics/failures`,
  `failure_queue`): recently `BLOCKED`/`EXECUTION_FAILED` runs, newest-first,
  bounded to a recent window (`days`, default 7), each with how long ago it failed
  and why (the reason from the `RUN_BLOCKED`/`AGENT_FAILED` audit event) — the
  "what broke and needs a human" cut. Read-only; a blocked run stays blocked
  (invariant #7).
- One run targets one repo. The parent/child run model + epic rollup (#35) has
  landed (`epics.py`, `foundry_runs.parent_run_id`, `GET /runs/{id}/epic`), and
  the epic's full chain now exports as one cross-run evidence pack
  (`build_epic_evidence_pack`, `GET /runs/{id}/epic/evidence`), and the dashboard
  surfaces epics as a rolled-up board (`GET /epics`), and the *producer* —
  decomposing an epic ticket into independently-gated child plans — has landed
  (`engines/decomposition.py`, `orchestrator.intake_epic`), and the webhook/trigger
  intake path can now opt into running it automatically (`epics.auto_decompose`,
  default off, wired through `InlineDriver`), and LLM-assisted decomposition has
  landed (`engines/llm_decomposition.py`, `decomposition.provider: llm`) — it
  recovers prose-described epics over the deterministic floor (additive,
  grounded, degrade-to-floor), so the producer is no longer deterministic-only.
  Path-scoped policy has two slices now: per-repo forbidden-path
  globs (`policy.repo_forbidden_globs`) make the sticky forbidden-path block
  monorepo-aware (additive, no Rego), and per-repo **required approval roles**
  (`policy.repo_required_roles`, issue #31) let a repo demand specific approval
  roles regardless of risk (additive, lock-step Rego). The **N-of-M approval
  matrix** (the "two-person rule") has landed too (`policy.min_approvals` +
  per-repo `policy.repo_min_approvals`, issue #31): a run needs that many
  *distinct* human approvers before it leaves `WAITING_APPROVAL` for `APPROVED`
  (approvals accumulate, each its own audited sign-off, a duplicate from the same
  person refused). It is enforced in the orchestrator lifecycle — like the
  forbidden-path block, **no policy-engine/Rego rule**, so invariant #2 doesn't
  apply — and is strictly additive (default 1 = the historical single-approval
  path; a per-repo value only ever raises the floor via `max`, never lowers it).
  When the effective count is >1 the intake **approval prompt** (tracker comment +
  Slack message) surfaces it ("Approvers required: N distinct sign-offs"), so the
  first approver is told up front that one sign-off won't release the run rather
  than discovering it as a still-parked run; default 1 renders no such line. And
  once a *partial* sign-off lands, the run no longer goes silent: `approve()`
  fires a **mid-flow progress nudge** (`RunNotifier.approval_progress` +
  `format_approval_progress_comment`, issue #31) re-pinging the next approver on
  the tracker + chat ("N of M distinct sign-offs collected — K more required");
  presentation-only (no gate/audit change), and `min_approvals=1` never reaches
  the partial branch, so the single-approval path is byte-for-byte unchanged.
  A **starter policy library** has landed too (`policy/library/`,
  `foundry-policy` CLI, issue #31): vetted `baseline`/`soc2`/`change-management`/`pci-dss`
  presets a buyer copies into `foundry.yaml` — built only from existing knobs,
  copy-to-adopt (never auto-applied), so it adds no gate mechanism. The library
  is now also **checkable**, not just copyable: `foundry-policy check
  --against <preset-or-path>` (`compare_policy_strictness`) verifies a
  deployment's own config is at least as strict as a baseline on every gated
  knob and **exits non-zero when it is weaker** — a read-only compliance-CI gate
  that closes the adopt-then-drift gap, adding no policy mechanism. The dashboard
  runs that **same check continuously** (`GET /metrics/policy/check` + a
  compliance panel, issue #31): set `dashboard.policy_baseline` (a preset name or
  config path) and the live gate's control-by-control PASS/FAIL against that
  baseline is visible in-app — the always-on twin of the CLI gate, computed from
  the same `compare_policy_strictness` (shared `comparison_to_dict`), so drift
  below a committed baseline shows on the dashboard, not just in a one-shot CI
  run; default unset = no check, an unresolvable baseline fails loud at startup.
  **Per-*path*
  (sub-tree) approval roles** have landed too (`policy.path_required_roles`,
  issue #31/#35): a path glob → required approval roles, evaluated **diff-aware**
  in the orchestrator PR re-check (`_unapproved_path_roles`) — a diff touching a
  configured subtree whose role is not already covered by the run's approvers
  escalates the run to `REVIEW_REQUIRED` (audited `risk.escalated`, category
  `path_required_roles`). Like the forbidden-path block it is orchestrator-only
  and strictly additive (it can only ever *escalate* to human review, never
  release a run — invariant #1), so it adds **no policy-engine/Rego/gate rule**
  (invariant #2 doesn't apply); default empty = unchanged. **Change-freeze /
  maintenance windows** are the *time* axis of the same family
  (`policy.change_freeze_windows`, issue #31): recurring-weekly or absolute
  windows (`policy/freeze.py`) consulted before an autonomous re-dispatch in
  `_attempt_remediation` — during an active freeze the auto-retry is held and the
  run escalates to `REVIEW_REQUIRED` (audited `risk.escalated`, category
  `change_freeze`), so no code ships unattended during a release blackout. Same
  shape as the per-path rule: orchestrator-only, strictly additive (a freeze only
  ever holds work for a human, never releases it), surfaced in `explain` /
  `check` / the dashboard policy panels; default empty = unchanged; the initial
  human-approved dispatch is not gated. **Live user-loadable** OPA bundles and
  **custom risk categories** remain the open items on the programmable-policy
  epic (#31).
- No Slack/Teams surface; approvals are tracker comments or the REST endpoint.

A C#/.NET port of the core lives in unmerged PR #1 (`dotnet/`); the Python
implementation is canonical — genuine defects get fixed in both.
